import Firebase from 'firebase';
import FireProof from 'fireproof';

const firebaseRef = new Firebase('https://relate-chat.firebaseio.com');
const ref = new FireProof(firebaseRef);

function getUsers(relaterUid) {
  return;
};

function getMessages(relaterUid) {
  return;
};

export default DashData = {
  // getRelaterData: 
};
